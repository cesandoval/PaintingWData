container_commands:
  00-enable-epel:
    command: sudo yum-config-manager --enable epel
  00-install-redis:
    command: sudo yum install redis
  01-start-redis:
    command: sudo systemctl start redis
  02-redis-on-boot:
    command: sudo systemctl enable redis
