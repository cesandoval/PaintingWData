extends layout

block pageTitle


    .container.spacing-large(id='progress-widget')
        .row.spacing-large
        h1 progress widget, click to start polling
        .row-spacer
            div.text-center
                    button.btn.btn-block.btn-gray.btn-jumbotron(id='PollingToggle') Start Polling

        h2 jobs in progress
        .row-spacer
        div(id='jobList')
        span Job Progress Section
        ul#progressList(style='list-style-type: none; width: 300px;')
                    //- -for( var i = 0; i < j; i++)
                    //-     -var job = currentJobs[i]
                    //-     +progressBar(...job)


    //- TODO, refactor this to a widgitized state. We 
    //- mixin progressBar(id, title, numerator, denominator)
    //-     -var percentage = Math.floor(((numerator/denominator)*100)).toString() + "%";
    //-     -var width = "width: " + percentage;
    //-     li.job-container
    //-         span id: #{id}      title: #{title}
    //-         .progress-container
    //-             .progress-bar.progress-bar-success(role='progressbar',aria-valuenow="40",
    //-             aria-valuemin="0", aria-valuemax="100", style="#{width}")
    //-                 = numerator + "/" + denominator
    
    script(src="/javascripts/progressWidget.js", type="text/javascript")

        
