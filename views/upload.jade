extends layout

block pageTitle
  div.container
      .row.spacing-large
         h1.center Upload Data
      .row.spacing-large    
        .row
          .col-md-8
            h2 SHP files must be compressed in a ZIP file. <br>The ZIP file should include these files:
            p .dbf file
            p .shp file
            p .shx files
            p Do not include /shd.xml file in the ZIP
        hr
        .row
          .col-md-8
            input(id="file-selector",  name = "zip_file", type="file")
            br
            p.spacing
              a(class="btn btn-lg btn-gray btn-jumbotron hidden", id="upload", name="upload", role="button", style="width: 100px") Upload
        .row.spacing
          div.progress.hidden(style="backround-color: white")
            div(class="progress-bar progress-bar-warning" role="progressbar", style="width: 100%").
              Uploading File ...

  script(src="/javascripts/js/fileUpload.js")
          
          


